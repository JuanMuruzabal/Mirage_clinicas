# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ⚠️ ZONA EN OBRA — Prisma Engine (plan de personalización de página, en curso)

**Estado del aviso: ACTIVO.** Se mantiene hasta que **todas las filas de la tabla de abajo estén en ✅**, o hasta que Kevin lo cancele explícitamente. Quien mergee un PE actualiza la tabla en ese mismo PR; quien mergee el último borra esta sección entera y el puntero de la Fase 4.

Kevin (`Kevinmass`) tiene un plan para reescribir la vertical de personalización de la página pública: `docs/Fases post MVP/Prisma Engine/plan-prisma-engine.md`. **Todavía no está implementado**, y lo que ese plan marca **[SUJETO A REVISIÓN]** puede cambiar si Juan opina distinto. Este aviso existe para que nadie construya sobre algo que el plan va a reemplazar, o lo pise, antes de que Kevin lo termine.

**Instrucción para Claude Code (obligatoria).** Antes de editar, borrar o mover cualquier archivo o área de la lista "Áreas cubiertas", **frená y avisale al usuario** con algo como: *"Ojo: esto toca una zona del plan Prisma Engine de Kevin (PE-N). Puede chocar con su trabajo, o quedar obsoleto cuando entre PE-1. ¿Seguimos igual, o lo hablás antes con Kevin?"* — y esperá la respuesta. **No es un bloqueo**: si el usuario confirma, seguí, pero dejá dicho en el PR qué área del plan tocaste. Antes de empezar, corré `git fetch --prune`, `git branch -r --list "origin/*pe-*"` y `gh pr list --state open --search "Prisma Engine"` para ver si ya hay ramas o PRs del plan abiertos sobre esos archivos.

**No hace falta avisar** si la rama actual es `feature/pe-*` o `docs/prisma-engine*` (es trabajo del propio plan), ni si el usuario dice explícitamente que está trabajando sobre el plan.

**Áreas cubiertas** (por el plan, en cualquiera de sus PRs):
- **Frontend, página pública y editor:** `apps/web/src/lib/pagina-publica/`, `apps/web/src/lib/temas-pagina-publica/`, `apps/web/src/components/editor-pagina/`, `apps/web/src/components/pagina-editor.tsx`, `apps/web/src/components/public/` (la plantilla y `modulos-publicos.tsx`), `apps/web/src/app/personalizar-pagina/`, `apps/web/src/app/[slug]/`, `apps/web/src/app/uploads/`.
- **Frontend, perfil y transversales:** `apps/web/src/app/perfil/` (aval del profesional y foto de perfil), `apps/web/src/components/scroll-reveal.tsx` y `expandable-card.tsx` (la dependencia `framer-motion`), `apps/web/src/middleware.ts` (CSP), `apps/web/next.config.ts`, las dependencias `zod` y `framer-motion` de `apps/web/package.json`.
- **Backend:** `apps/api/internal/http/pagina_publica.go` (validación de módulos y temas, `PATCH /panel/pagina`), `clinicas.go` (el `GET` público), `me.go` (perfil), `apps/api/internal/storage/` y `buildStorage` en `cmd/api/main.go`.
- **Modelo de datos:** `PaginaPublica`, `PaginaPublicaModulo`, `ClinicMember` (columna de aval), `ProfessionalProfile` (foto y descripción) y la tabla nueva `horarios_clinica`; y por eso `apps/api/internal/db/migrate.go` y `migrate_una_vez.go` en lo que toque esas tablas.
- **Estructura nueva:** `packages/prisma-engine/` y `apps/api/internal/prismaengine/` (los crea PE-1), el script `pnpm engine:generar`, y los cambios de `pnpm-workspace.yaml`, `apps/web/Dockerfile`, `render.yaml` y `.github/workflows/ci.yml` que PE-1 necesita.

**Qué evitar mientras el aviso esté activo:**
- **No sumes tipos de módulo, temas, variantes ni tipografías por el camino de hoy** — desde PE-1 (parte 1, mergeada) viven en `packages/prisma-engine` (`registro.ts`, `schemas.ts`, `catalogo/temas.json`), no en un `switch` de `editor-de-modulo.tsx`/`modulos-publicos.tsx` ni en un mapa a mano de Go: sumar uno tocando solo esos archivos evita el problema que PE-1 vino a resolver. Si es urgente, coordinalo con Kevin.
- **No cambies la semántica de `PATCH /panel/pagina`** (hoy guardar = publicar) ni la de `deployada_en`: es de PE-8.
- **No agregues columnas ni migraciones** sobre `paginas_publicas`, `pagina_publica_modulos`, `clinic_members` ni `professional_profiles` sin revisar el plan: chocan con `schema_version`, con el aval del profesional y con `horarios_clinica`.
- **La subida de foto de perfil** deja de estar fuera de alcance solo si Kevin y Juan confirman esa decisión (marcada **[SUJETO A REVISIÓN]** en el plan). Hasta entonces sigue fuera.
- Los arreglos chicos (un bug de una línea, un texto) están bien: avisá igual, y si podés, dejalos en un commit aparte.
- **Numeración de TR:** Kevin va a usar TR-161 en adelante para las decisiones del plan. Si Juan necesita una TR nueva antes de que eso esté mergeado, que coordine el número para no chocar.

**Estado de los PRs del plan** (⬜ pendiente · 🔄 en curso, con la rama · ✅ mergeado a `dev`):

| PR | Qué | Estado |
|---|---|---|
| PE-1 | Núcleo: registro único de módulos y catálogo compartido | 🔄 (rama `feature/pe-1-nucleo-motor`) |
| PE-8 | Borrador, Publicar = Deployar e historial | ⬜ |
| PE-2 | Tokens de diseño | ⬜ |
| PE-3 | Variantes por módulo | ⬜ |
| PE-4 | Motor de efectos | ⬜ |
| PE-5 | Fondos tranquilos y texto avanzado | ⬜ |
| PE-6 | Módulos del rubro (Equipo, Horarios, Servicios y contenido) | ⬜ |
| PE-7 | Plantillas y presets | ⬜ |
| PE-9 | SEO, compartir y rendimiento | ⬜ |
| Fase 4.6 | Storage real en producción (R2), dependencia externa | ⬜ |

## Qué es esto

PRISMA: plataforma para odontólogos de Córdoba que combina gestión de clínica (turnero, agenda, pacientes) con una página pública propia deployable donde los pacientes piden turno, más un buscador público de clínicas. La especificación completa vive en `docs/Arquitectura y base/dental-mirage-spec.md`; el plan de implementación fase por fase en `docs/Arquitectura y base/implementation-plan.md`; las decisiones de arquitectura/alcance con sus alternativas descartadas en `docs/Arquitectura y base/tradeoffs.md`. Leé esos tres antes de planificar o tocar un módulo nuevo — son la fuente de verdad, este archivo es solo el mapa rápido. El sistema de auth/onboarding (Google OAuth, verificación de mail por código de 6 dígitos, clínicas individuales/organización) queda documentado como cierre de feature en `docs/Login/feature-sumarte-login-resumen.md` (qué se implementó, variables de entorno, repaso de seguridad) y como decisiones puntuales en `docs/Arquitectura y base/tradeoffs.md` TR-036 a TR-047 — el flujo post-TR-047 cambió: `/sumarse` es solo crear cuenta + confirmar código (2 pasos), perfil profesional y clínica se completan después en un modal de "bienvenida" sobre `/seleccionar-servicio` (TR-057 a TR-059) — `docs/Login/feature-sumarte-login.md` fue el prompt original de esa feature, no un documento vivo, y puede quedar desactualizado. `README.md` (raíz) tiene la puerta de entrada para levantar el proyecto de cero (setup, comandos, CI, deploy).

**Proyecto de referencia:** `../Marcuzzi_Madryn` — mismo stack y modo de trabajo, reutilizado sin cambios (spec §9). Ante cualquier duda de patrón (BFF, interfaces dev/prod, testing, flujo de ramas), ese repo es el ejemplo ya validado en producción de cómo aplicarlo.

## Stack (decidido, no "a evaluar" — spec §9.1)

El **porqué técnico** de cada pieza —contra qué alternativa se comparó, qué se sacrifica y qué condición la haría cambiar— está en `docs/Arquitectura y base/por-que-cada-decision-tecnica.md`. Leerlo antes de proponer cambiar cualquier elemento del stack: varias decisiones que parecen arbitrarias sostienen mecanismos centrales (el exclusion constraint de no-solapamiento y el rollback de las migraciones dependen de features que solo tiene PostgreSQL).

- **Backend:** Go + [chi](https://github.com/go-chi/chi) + GORM, en `apps/api`.
- **Frontend:** Next.js + TypeScript + Tailwind, en `apps/web` — leer el `AGENTS.md` que genera cada versión de Next antes de escribir código, por posibles breaking changes.
- **DB:** PostgreSQL + extensión `btree_gist`.
- **Package manager JS:** `pnpm` (no `npm`).
- Requiere Docker corriendo (`docker compose up -d`) para Postgres local.

## Comandos

Desde la raíz del repo (`package.json` tiene los atajos; detalle completo en `README.md`):

- `pnpm run dev:web` / `pnpm run build:web` / `pnpm run lint:web` / `pnpm run typecheck:web`
- `pnpm run typecheck:shared-types`
- `pnpm run typecheck:prisma-engine` / `pnpm run test:prisma-engine` — el registro único de módulos de la página pública (`packages/prisma-engine`, plan Prisma Engine PE-1).
- `pnpm run engine:generar` — exporta el esquema (zod) de cada módulo a JSON Schema y copia esquemas + catálogo de temas a `apps/api/internal/prismaengine/` (`go:embed` no puede leer fuera de su propio módulo). Correrlo después de tocar `packages/prisma-engine/src/modulos/*/schema.ts` o `catalogo/temas.json` — CI lo corre y falla si el resultado difiere de lo commiteado (job `web`).
- `pnpm run dev:api` (`go run ./cmd/api`) / `pnpm run build:api` (`go build ./...`) / `pnpm run vet:api`
- `pnpm run migrate:api` — aplica el esquema (AutoMigrate de GORM). Idempotente, se puede correr de nuevo sin romper nada.
- Lint de Go: `golangci-lint run ./...` dentro de `apps/api` (no tiene atajo en `package.json`). En Windows con `git config core.autocrlf=true` (y sin `.gitattributes` en el repo), el working tree queda en CRLF mientras los blobs de git son LF — `gofmt`/`golangci-lint` corridos en el lugar dan resultados inestables (archivos distintos marcados "mal formateados" de una corrida a otra, puro ruido CRLF-vs-LF). Antes de confiar en un resultado ahí: extraer una copia sin CRLF primero (`cp -r apps/api <tmpdir> && find <tmpdir> -name '*.go' -exec sed -i 's/\r$//' {} \;`) y correr el lint dentro de esa copia.
- `pnpm run test:web` (Vitest) / `pnpm run test:api` (`go test ./internal/...`) — ver "Testing" abajo.
- `pnpm run test:coverage:web` / `pnpm run test:coverage:api` — mismo run + reporte de cobertura.
- Un test puntual: frontend `pnpm --filter @dental-mirage/web test <patrón del archivo o nombre>`; backend `cd apps/api && go test ./internal/<paquete>/... -run TestNombre`.
- `-race` (detector de condiciones de carrera en Go) no está en `test:api` porque alarga la corrida, **pero CI sí lo corre** (job `test-api`): un test que pasa local puede fallar ahí por una carrera. Para correrlo local: `cd apps/api && go test ./internal/... -race`.
- `internal/testdb` crea la base `dental_mirage_test` sola si no existe (alcanza con Postgres arriba); solo hay que setear `TEST_DATABASE_URL` si se usa otra.
- `packages/shared-types` son interfaces TS escritas **a mano** que reflejan los structs de respuesta de `apps/api`: no se generan. Si cambiás la forma de un JSON del backend, actualizalas a mano — `typecheck:shared-types` solo detecta lo que el frontend ya consume.
- `scripts/qa-entorno-dev.sh` recorre el flujo real (onboarding → turno → panel → wizard público) contra los contenedores levantados con `docker compose up -d --build`; complementa la suite, que corre sobre transacciones revertidas y no ve problemas de migración o configuración reales.

Flujo de ramas activo desde antes de Sprint 4 — ver "Flujo de ramas" más abajo; no asumir que hay que crear una rama ni mergear a `main` sin que lo pidan explícitamente.

## El requisito no negociable: turnos sin solapamiento (spec §4.3)

Dos turnos de un mismo profesional no pueden solaparse en horario. Se garantiza con `EXCLUDE USING gist` sobre **`(atendido_por_user_id, rango_horario)`** en la tabla `turnos`, **a nivel de base de datos**, no solo validación de app — mismo mecanismo que las reservas de alojamiento/vehículos en Marcuzzi_Madryn. El constraint solo aplica a turnos en estado `agendado` (un `cancelada` nunca conflictúa, libera el horario). Hasta Extra 2.3.3 también excluía a los `pendiente` (recién llegados de la página pública, sin hora fija todavía — TR-006 en `docs/Arquitectura y base/tradeoffs.md`) — ese estado se eliminó del todo (TR-104): hoy `Turno.Estado` solo puede ser `agendado`/`cancelada`. Cualquier intento de solapamiento debe fallar de forma controlada (error de validación en el modal), nunca como 500 crudo.

**La columna cambió en la Fase 3.2.1 (TR-137), y el texto de esta regla no.** Siempre dijo "del mismo profesional", pero la constraint estaba sobre `profesional_id` — que guardaba un `clinics.id`. Mientras cada clínica tuvo un solo profesional las dos cosas coincidían; con N profesionales, sobre la clínica pasaba de garantía a **bloqueo falso** (rechazaba que dos odontólogos atendieran a la misma hora en sillones distintos). Sobre `atendido_por_user_id` gana además algo que el modelo viejo no podía expresar: impide que un profesional tenga turnos solapados en **dos clínicas distintas**.

Dos cosas que hay que saber al tocar turnos: **todo turno `agendado` debe tener `atendido_por_user_id`** (`chk_turno_agendado_profesional`) — sin esa columna el constraint no aplica, porque compara `WITH =` y dos NULL nunca son iguales; y ese usuario **tiene que ser miembro de esa clínica**, garantizado por la FK compuesta contra `clinic_members`. Para resolver el profesional cuando todavía no hay elección explícita, `db.OwnerDeLaClinica(tx, clinicID)`.

## Arquitectura del frontend: BFF con Server Actions — el navegador nunca llama a la API Go directo

Mismo patrón que Marcuzzi_Madryn: `apps/web/src/lib/api.ts` es `server-only`, sin prefijo `NEXT_PUBLIC_`. Todo el tráfico hacia `apps/api` pasa por Server Actions (`apps/web/src/app/actions/*.ts`, `"use server"`) o Route Handlers. La sesión (token opaco validado server-side contra `internal/auth.Session` en Go — ya no JWT stateless, ver TR-037 en `docs/Arquitectura y base/tradeoffs.md`) va en cookie `httpOnly` (`lib/session.ts`), nunca en `localStorage`. Una pantalla nueva extiende `lib/api.ts` y se llama desde Server Component/Action — nunca un `fetch` nuevo desde Client Component.

La sesión y el rate-limiting de auth (`internal/ratelimit`) viven en Postgres, no en memoria del proceso — cualquier instancia del backend valida/cuenta contra la misma tabla, así que escalar a varias instancias no requiere sticky sessions ni un secret compartido entre ellas. Dato aparte para no confundirse: la env var `JWT_SECRET` es un nombre heredado de antes de TR-037 — hoy solo firma el `state` de OAuth de Google (`internal/http/oauthstate.go`), no queda ningún JWT de sesión real; no se renombró para no romper deploys ya configurados con ese nombre (ver el comentario en `apps/api/.env.example`).

## Interfaces con implementación dev/prod (spec §9.4)

Mismo patrón que Marcuzzi_Madryn para cada dependencia externa: interfaz en Go, implementación dev (no-op/log) e implementación prod activada por env var, inyectada desde `main.go`. El envío inicial del formulario público de turno **no** usa este patrón — es un link `wa.me` generado client-side (TR-003), no una integración de backend; el patrón dev/prod queda reservado para recordatorios automatizados futuros (Fase 2) y para las dependencias del auth nuevo, ya implementadas: `internal/mail` (Resend/`LogSender`), `internal/googleauth` (Google OAuth), `internal/turnstile` (CAPTCHA), `internal/security` (HaveIBeenPwned) — todas nil-safe/no-op sin la env var correspondiente configurada, ver `buildAuthDeps` en `cmd/api/main.go`. `internal/storage` (foto de perfil) solo tiene la implementación dev (disco local) hecha — R2/S3 prod queda pendiente, ver TR-046 en `docs/Arquitectura y base/tradeoffs.md`.

## Timezone

Cualquier regla de fecha/hora "vigente" pasa por `internal/clock.Today()` fijado a `America/Argentina/Cordoba` (UTC-3), nunca `time.Now()` directo.

## Testing (gate real desde el día 1)

- Backend: tests contra Postgres real (no mocks), cada uno en su propia transacción revertida, `TEST_DATABASE_URL` debe terminar en `_test`.
- `go test ./internal/...` corre los paquetes en paralelo contra esa misma base de test, y cada paquete llama a `testdb.New(t)` → `db.RunMigrations` por su cuenta — por eso `RunMigrations` toma un `pg_advisory_xact_lock` antes de correr (serializa las llamadas concurrentes; ver el comentario grande en `apps/api/internal/db/migrate.go`). Sin ese lock, dos llamadas entrelazando los pasos de una migración cruda no atómica (`DROP CONSTRAINT` + `ADD CONSTRAINT`) pueden fallar con "ya existe" — bug real de CI, no hipotético. Cualquier SQL crudo nuevo que se agregue a esa función queda protegido por el mismo lock automáticamente.
- Frontend: Vitest + Testing Library + jsdom, todo mockeado a nivel `fetch`/Server Actions. El gate de cobertura excluye `src/app/**/page.tsx` (Server Components async, no testeables vía Testing Library/jsdom — quedan cubiertos por QA end-to-end manual, no por esta suite).
- **Gate: 80% de cobertura mínimo en ambos lados, activo desde Sprint 0** (a diferencia de Marcuzzi_Madryn, donde se agregó después — ver TR-007 en `docs/Arquitectura y base/tradeoffs.md`), CI falla si no se cumple.

## Alcance del MVP vs. Fase 2

MVP (spec §2): alta de profesional individual + gestión de clínica completa (calendario, turnos, pacientes) + página pública con plantilla fija (sin editor visual) + buscador público. Login social (Google) y CAPTCHA (Turnstile) ya no están fuera de alcance, implementados (ver `docs/Arquitectura y base/tradeoffs.md` TR-036 en adelante y `docs/Login/feature-sumarte-login-resumen.md`). Cuentas de organización multi-profesional tampoco: el modelo de datos (`Clinic`/`ClinicMember`/roles) ya las soporta, aunque el envío/aceptación de invitaciones y el panel de gestión de miembros siguen sin implementar (fuera de alcance de esa misma feature, spec §9 — ver TR-044 en `docs/Arquitectura y base/tradeoffs.md`). **Fuera de alcance salvo pedido explícito:** subdominios propios, editor visual drag & drop, perfil completo de "Sobre nosotros", historia clínica/presupuesto con funcionalidad real, recordatorios automatizados por WhatsApp/SMS, 2FA, subida de foto de perfil (TR-046), CSRF por tokens dedicados — no las implementes solo porque aparecen como preparación futura en la arquitectura (ver `docs/Arquitectura y base/implementation-plan.md` sección 6).

**Fase 2 (2026-08-29 a 2026-09-07, completa):** brief del cliente en `docs/Fases post MVP/Fase 2/fase2-dental-mirage.md`, plan fase por fase en `docs/Arquitectura y base/implementation-plan.md` §11, decisiones de arquitectura en `docs/Arquitectura y base/tradeoffs.md` TR-078 a TR-120, resumen en `docs/Arquitectura y base/dental-mirage-spec.md` §11. **Brief extra post-QA (2026-09-06, 5 ítems más):** `docs/Fases post MVP/Fase 2/fase2.3-extra-dental-mirage.md`, plan en `docs/Arquitectura y base/implementation-plan.md` §11.5, orden confirmado por el cliente 2.3.1 → 2.3.2 → 2.3.5 → 2.3.3 → 2.3.4. **2.3.1 y 2.3.2 implementados y mergeados a `dev`** (TR-094 a TR-099, PR #5 y #6): rediseño completo del dashboard "Turnero" (6 tarjetas, deep-link real al calendario/turnos) + banner de conflicto en el calendario, más — pedido por el cliente durante esa QA, fuera del brief original — Autoreservar turnos, excepciones de horario de atención visualizadas en el calendario y Preferencia de atención por tipo de consulta. **2.3.5 implementado y mergeado a `dev` (TR-100 a TR-103, PR #7):** el formulario público reescrito de punta a punta — datos de contacto → "Confirmanos que sos vos" (código de 6 dígitos al mail, TR-103, pedido extra del cliente durante esta misma QA) → tipo de consulta → fecha → horario disponible (mismo cálculo real que usa el profesional, TR-086) — el turno nace `agendado` con horario fijo, nunca más `pendiente`. Suma unicidad de DNI por clínica en `Paciente` (índice único + reuso de ficha existente en vez de duplicar, TR-100) — con verificación explícita en "Agregar turno" y sincronización forzada de los datos reales del paciente cuando el DNI ya existe, para que nadie pueda "generar" un turno con datos ajenos usando el DNI de otra persona (TR-101) — y un botón "+ Agregar paciente" para alta directa sin pasar por un turno (TR-102). **2.3.3 y 2.3.4 implementados, aprobados y mergeados a `dev` (TR-104, PR #8):** el estado `pendiente` de `Turno` se elimina del todo (backend: check constraint estrechado a `agendado`/`cancelada`, `agendarTurnoHandler`/`editarTurnoHandler` borrados; frontend: se saca la pestaña de pendientes de `/panel/turnos`, "Confirmadas" queda autoseleccionada al entrar sin `?estado=`) — en Turnos, motivo largo pasa a "Ver motivo" (editable solo si `origen === "manual"`), botón "Ver paciente" en cada fila, filtro rápido HOY/SEMANA/MES antes de Desde/Hasta (sumado de cero a `/panel/turnos`, que no tenía filtro de fecha; sumado también a "Turnos activos" de la ficha de paciente, que ya tenía Desde/Hasta — sacado de "Historial de turnos" a pedido del cliente), filtro y columna de tipo de consulta (con "Ver tipo →" si el nombre es largo), y "Todas" repuesta como pestaña elegible a mano (el default sin `?estado=` sigue siendo "Confirmadas") — y en Pacientes, mail largo pasa a "Ver mail" (mismo componente `VerTextoBoton` de TR-094/E1.7, ahora también en "Datos de contacto" de la ficha y, como link con flecha, en la columna Contacto de Turnos). Varias rondas de corrección de QA sobre la primera entrega quedan documentadas en el propio TR-104. Calendario mobile con encabezados que acompañan el scroll (ítem 1), ajustes de horario de atención/tipos de consulta/horarios reservados generales y específicos — **sin previsualización en vivo**, descartada explícitamente por el cliente (TR-084) — con disponibilidad real calculada contra horario de atención/horarios reservados/turnos ya agendados (TR-086, reemplaza el campo de hora libre de la primera entrega), y compartir calendario por link efímero — **implementado y mergeado a `dev` (TR-120, PR #15)**: reescrito respecto al brief original como tercera pestaña de "+ Agregar turno" ("Compartir link"), no un botón aparte; link de 1h sin CAPTCHA/código de verificación/detecciones de abuso (conflictos de identidad y tope de 1 turno activo por DNI sí se mantienen), que se agota por uso — un turno "para mí" lo apaga de inmediato, "para otro" tiene cupo de 5 — decidido por quien lo usa en la pantalla "¿Para quién es el turno?" de siempre, no por el profesional al generarlo. Corrección de QA post-merge (mismo TR-120, addendum): la pantalla "¿Ya te atendiste?" se saltaba entera con enlace en la primera entrega — se restauró para que el wizard con enlace recorra exactamente los mismos pasos que sin él, incluido el chequeo retroactivo de "primera vez" contra fichas ya verificadas. La vista "pantalla grande" del calendario en mobile (ítem 2) se **descartó por completo** tras fallar en dispositivo real (TR-085) — no existe en el código. Horarios reservados que se solapan (entre sí o con un turno) se agrupan en una tarjeta única "Ver eventos →" (TR-089/090, reemplaza cualquier diseño anterior de "turno rayado"); un turno en conflicto que ya pasó pasa a "resuelto" sin el aviso urgente. No se puede reservar un horario en el pasado, hay un acceso rápido "+ Reservar horario" junto a "+ Agregar turno", y un específico ya vencido se elimina directo desde el calendario (TR-091). Un turno resuelto admite marcar asistió/ausente — **irreversible**, con aviso de confirmación y rechazo del backend ante cualquier intento posterior (TR-092).

**Fase 2.4 — formulario público reescrito de punta a punta, implementada y mergeada a `dev` (TR-100 a TR-116, PR #9 a #14):** el wizard público distingue paciente "verificado" (al menos un turno resuelto y asistido, o ficha cargada a mano por el profesional) de "no verificado", ofrece el camino "ya he venido antes" (tarjeta clickeable, sin retipear datos) y detecta conflictos de identidad (mismo DNI, mail distinto) dejándolos pendientes de resolución manual del profesional — con 3 detectores de abuso (tope de 1 turno activo por DNI, mail con muchos DNIs distintos, rotación por IP) que no afectan a un paciente ya verificado. **Fase 2.4.2 — "sacar turno para otro" (tutor/representante), TR-116:** un tutor puede reservar el turno de otra persona verificándose él mismo por mail — el paciente sigue siendo el dueño del turno/historial; un paciente puede tener más de un tutor a lo largo del tiempo (`PacienteTutor`, uno-a-muchos). Detalle completo del mecanismo (qué hace cada detector, qué testear) en `docs/Fases post MVP/Fase 2/turnero_pagina/ArquitecturaPeticionesTurno.md`.

**Fase 2 completa (2026-09-07):** los 5 ítems del brief original, los 5 ítems extra de F2.3 y los ítems extra de Fase 2.4/2.4.2 quedan implementados, aprobados por el cliente y mergeados a `dev` — lista para pasar a `main`. Ver "Flujo de ramas" abajo — esta fase tuvo un proceso de QA propio, distinto del criterio general.

## Auditoría de seguridad y optimización (línea de trabajo recurrente)

Desde el 2026-09-08, después de cerrar la Fase 2, el sistema entra en rondas periódicas de revisión técnica. **Antes de tocar `internal/http`, `internal/db` o `apps/web/src/lib/api.ts`, leer `docs/Seguridad y optimizacion/radiografia-tecnica_1.md`** — es el diagnóstico vigente y explica por qué varias cosas están como están.

- `docs/Seguridad y optimizacion/radiografia-tecnica_1.md` — el diagnóstico (primera radiografía, de ahí el `_1`), el plan de acción en Fases A/B/C, y el registro de cada ronda de arreglos.
- `docs/Seguridad y optimizacion/como-se-arreglo-cada-cosa.md` — el porqué de cada decisión y las alternativas descartadas.
- Decisiones de arquitectura: `docs/Arquitectura y base/tradeoffs.md` TR-121 a TR-132. Plan por fases: `implementation-plan.md` §12. Resumen: `dental-mirage-spec.md` §12.

**Fases A y B cerradas (2026-09-09).** Lo que hay que tener presente al escribir código nuevo:

- **`clientIP()` prefiere `CF-Connecting-IP`, y si no está, la última IP PÚBLICA de `X-Forwarded-For`** (TR-136, corrige TR-121). El "último valor" de TR-121 asumía un solo proxy delante y **nunca fue cierto**: Render pone Cloudflare delante de todos sus servicios y además tiene un router interno — medido, un request quedaba logueado con `ip=10.29.215.4`, una dirección privada. El logger escribe `ip_fuente` (`cf`/`xff`/`xff-interna`/`remote`) en cada request: si alguna vez hay que revisar la topología de proxies, **mirá ese campo en un deploy real, no deduzcas**.
- **Las comodidades que exponen datos van detrás de `Config.HerramientasDeDesarrolloHabilitadas()`** (TR-135): exige `DEV_TOOLS=true` **y** que `AppBaseURL` apunte a localhost. Si agregás una comodidad de desarrollo nueva (exponer un dato, saltear un control, sembrar datos), va detrás de esa puerta — **no** de `APP_ENV`, que vale `development` en Render a propósito, ni de "¿está configurado X proveedor?", que fue el error que dejó `ExponerCodigoVerificacion` prendido dependiendo de una variable no relacionada. `AutoVerifyEmail` es la excepción y sigue atada a Resend: no expone nada, es una decisión de producto (TR-051).
- **La IP que ve `clientIP()` la pone el BFF** (TR-134, Fase 3.1.1). El navegador nunca llama a la API directo, así que sin esto llegaría siempre la IP del proceso web — la misma para todos los visitantes, que dejaba al rate-limiting por IP y a los detectores de abuso del wizard contando a todo el mundo como una sola persona (borró turnos reales en QA). `apps/web/src/lib/api.ts` manda `X-Prisma-Client-IP` + `X-Prisma-Bff-Auth`, y el middleware `confiarEnIPDelBFF` (`internal/http/ip_del_visitante.go`) reescribe con eso el `X-Forwarded-For` **solo si el secreto coincide** — la API es pública, sin esa prueba cualquiera elegiría su IP. Requiere `BFF_SHARED_SECRET` con el MISMO valor en los dos servicios; vacío, todo se comporta como antes. **Para leer la IP usar siempre `clientIP(r)`** — no leer esas cabeceras a mano, el middleware las borra a propósito.
- **`decodeJSON(w, r, v)` acota el body a 1 MiB.** No leer `r.Body` por fuera de esa función.
- **Paginación opt-in en `/turnos` y `/pacientes`** (TR-122): `?limit=&offset=`, total en `X-Total-Count`. Sin `limit` el endpoint responde como siempre — el calendario depende de eso, necesita el rango de fechas completo. Del lado del panel, "Cargar más" (`components/panel/cargar-mas.tsx`, tamaños en `lib/paginacion.ts`).
- **Nunca loguear la query string** (TR-124): "Mis turnos" lleva DNI y mail del paciente ahí. Se loguea el patrón de ruta de chi, no la URL concreta.
- **Las migraciones de DATOS van en `aplicarUnaVez`** (`internal/db/migrate_una_vez.go`, TR-123), no en el bloque idempotente de `migrate.go`. `RunMigrations` reintenta ante deadlock (SQLSTATE 40P01).
- **`JWT_SECRET` no tiene nada que ver con JWT** (TR-125): acá no hay ningún JWT ni la librería. Es el nombre heredado de la env var que firma con HMAC el `state` de OAuth de Google; del lado de Go se llama `Config.OAuthStateSecret`. El proceso no arranca fuera de `development` si su valor resuelto es el de ejemplo del repo.

**Fase C — lo que ya está aplicado (2026-09-09):**

- **El esquema tiene foreign keys reales** (TR-131): 34 constraints (33 desde la auditoría de seguridad, +1 de `pagina_publica_modulos` en la Fase 4.1). Dos cosas que hay que saber antes de tocar el modelo de datos:
  - **La columna que apunta a la clínica se llama `clinic_id`** en todo el esquema desde la Fase 3.2.1 (TR-137). Se llamaba `profesional_id` en 9 tablas y guardaba un `clinics.id` desde TR-037 — el renombre se hizo antes de agregar `turnos.atendido_por_user_id`, para que no convivieran dos columnas de nombre parecido y significado opuesto. Si ves `profesional_id` en un documento viejo, es el nombre anterior de `clinic_id`. **Las tablas legacy `profesionales` y `profesional_especialidades` ya no existen** (13 filas huérfanas borradas el 2026-09-13; este archivo afirmaba que estaban vacías y era falso), y con ellas se fue `cmd/migrate-usuarios`.
  - **`conflictos_paciente` no lleva foreign key a propósito** — es historial, sus referencias quedan colgadas cuando se resuelve un conflicto. Hay un test que lo protege; no "arreglarlo".
  - **Una ficha de paciente se borra SOLO con `borrarFichaPacienteConSusHijas`** (`paciente_verificado_publico.go`), nunca con un `Delete(&db.Paciente{})` suelto. `pacientes` tiene tres tablas hijas (`paciente_tutores`, `paciente_emails_alternativos`, `paciente_telefonos_alternativos`) con FK `NO ACTION`: cualquier fila viva rebota el DELETE con 23503 y tumba la transacción. Regresión real de QA (2026-09-12), ver el addendum de TR-131.
- **Una migración que borra datos va en `migracionesDestructivasPrevias/Posteriores`** (`internal/db/migrate_destructiva.go`, TR-132), nunca suelta en el loop de `statements`. Declara cómo contar su impacto; fuera de `development` no corre sin `DB_ALLOW_DESTRUCTIVE=true`. Si el AutoMigrate falla cuando ella no corrió antes (un check constraint del tag de GORM, por ejemplo), va en el grupo PREVIO.

**Fase C — lo que sigue pendiente,** y no tiene fecha: cada ítem tiene una condición de activación escrita (`implementation-plan.md` §12.3). La refactorización de `turno_publico.go`/`turnos.go`/`pedir-turno-form.tsx` quedó postergada a la próxima radiografía — no refactorizar eso "de paso" sin pedido explícito.

## Fase 3 — Multi-tenant (N profesionales / N clínicas)

Brief del cliente en `docs/Fases post MVP/Fase 3/Fase2-fix-Fase3-Multi-tenant.docx`; mockups en `docs/Fases post MVP/Fase 3/Mockups/`; plan en `docs/Arquitectura y base/implementation-plan.md` §13.

**Fase 3.1 — cambios al wizard público, cerrada el 2026-09-12 (TR-133).** Explicación larga en `docs/Fases post MVP/Fase 3/fase3.1-wizard-sacar-turno.md`; mecanismo en `docs/Fases post MVP/Fase 2/turnero_pagina/ArquitecturaPeticionesTurno.md` §1.4quater. Lo que cambia respecto a lo que dicen las secciones de Fase 2 más arriba:

- **La regla vigente es 1 turno activo por DNI Y TIPO DE CONSULTA**, no 1 por DNI en toda la clínica. TR-109 queda revertido: `turnoActivoPorDNI`/`turnoActivoDeOtroTipo` siguen en el código sin call sites (`//nolint:unused`), vale `turnoActivoDelMismoTipo`. Cualquier texto anterior que diga "tope universal de 1 turno activo por DNI" describe el estado viejo.
- **Relajar el tope no quitó protección:** la identidad la protege la detección de conflictos (mismo DNI + mail distinto → dos fichas + conflicto a resolver a mano), que el tope tapaba. No "reforzar" esto con un tope nuevo sin pedido explícito.
- **La prueba de mail se reemite heredando su vencimiento** (`consumirYReemitirVerificacionTurnoPublico`). Sigue siendo de un solo uso; lo que no hace es reiniciar los 30 minutos. Si tocás esto, la herencia del `ExpiresAt` es la parte que no se puede perder.
- **`pacienteReconocibleEnElWizard` ≠ `pacienteEstaVerificado`.** El primero (verificado **o** con turno activo) decide si aparece la tarjeta de "ya he venido antes". El segundo, más estricto, sigue rigiendo todo lo demás. No unificarlos.
- **`GET /clinicas/{slug}/mis-turnos` devuelve una LISTA**, y sigue dando 404 —no 200 con lista vacía— cuando ninguno matchea.
- **En "para otro / primera vez" el código de verificación sale al terminar los datos del TUTOR**, no después de los del paciente. No es cosmético: con el mail probado, el wizard decide si ofrecerle sus pacientes ya conocidos (`otro-tarjeta-paciente`) o pedirle los datos de uno nuevo (`otro-paciente`). **Esa lista nunca puede mostrarse antes del código** — diría qué pacientes tiene un tutor a cualquiera que tipee su mail.
- **La búsqueda de pacientes de un tutor es por MAIL, nunca por teléfono.** El código se manda al mail: es lo único que la persona demuestra tener. Sumar el teléfono al criterio dejaría ver los pacientes de un tutor ajeno verificando la propia dirección. Requiere verificación por SMS, hoy fuera de alcance.

**Fase 3.2 — multi-tenant, en curso desde 2026-09-13.** Documento vivo: `docs/Fases post MVP/Fase 3/fase3.2-multi-tenant.md`. Modelo de datos con los diagramas ER pre y post: `docs/Arquitectura y base/modelo de datos/`. Plan por subfases (3.2.1 a 3.2.8) en `implementation-plan.md` §13.2 — **completas hasta la 3.2.7e y la 3.2.6 (vista del recepcionista, 2026-09-20/21, TR-160); solo queda la 3.2.8 (latencia y cierre)**. Lo que hay que saber antes de tocar el modelo:

- **La identidad del paciente es de la CLÍNICA; la lista de trabajo es de cada profesional** (Fase 3.2.5): son dos preguntas y dos endpoints. `/pacientes` acotado ("a quiénes atiendo yo"), `/pacientes/de-la-clinica` sin acotar ("¿esta persona ya está cargada?") — este último devuelve lo mínimo para reconocerla, no la ficha completa de un colega. Sin eso, cargar un turno tipeaba de nuevo a alguien que ya existía y el índice único de DNI rechazaba el alta.
- **Un paciente no puede tener dos turnos encimados, ni siquiera con profesionales distintos** (Fase 3.2.5): el `EXCLUDE` protege al profesional, no a la persona. `turnoSuperpuestoDeOtroProfesional` valida el RANGO dentro de la misma transacción, **en el alta, al reprogramar Y al AUTORESERVAR** (Fase 3.2.7e — `calcularDisponibilidad` mira la agenda del profesional, no la de la persona, así que autoreservar tomaba el primer hueco y creaba el encimado; era el último camino abierto). El 409 dice con quién y **de qué hora a qué hora**, y desde la 3.2.7 rige también en el **wizard público** (`turno_publico.go`).
- **Las dos reglas del paciente valen en el panel Y en el wizard** (Fase 3.2.7): el alta pública chequea las mismas dos cosas antes del insert, con mensajes en segunda persona. Si agregás un camino nuevo por el que nazca un turno, tiene que pasar por las dos.
- **Un paciente no puede tener dos turnos activos del MISMO tipo en la clínica** (`turnoActivoDelMismoTipoEnLaClinica`, 2026-09-15). La regla existía desde la Fase 3.1 pero solo en el wizard público —y ahí como control de abuso sobre `tipo_consulta_id`—; cargando a mano no se aplicaba ninguna. **Compara por NOMBRE normalizado** (TR-145): por id no vería nunca el turno del colega, que tiene su propia fila para el mismo tipo, que es justo el caso a detectar. Busca por `paciente_id` (el DNI del turno es un snapshot; la ficha es la identidad) y compara los nombres en Go, no en SQL — `normalizarNombreTipo` necesita `unaccent`, que no está.
- **El historial de un paciente se ve ENTERO, con su dueño** (Fase 3.2.5): la ficha trae todos los turnos —el paciente es de la clínica— y cada uno informa `atendidoPorNombre` y `esMio`. Los ajenos van en solo lectura y sin link (el destino está acotado: no encontraría nada). No confundir con el aislamiento: una cosa es que un dato sea ajeno, y otra que una operación legítima —resolver un conflicto de identidad— tenga consecuencias sobre lo ajeno. Lo primero se corta; lo segundo se declara (el modal avisa cuántos turnos de otros alcanza).
- **El aislamiento lo verifica un test, no la memoria**: `TestAislamiento_NingunaConsultaDelPanelSinAcotar` lee los 15 archivos del panel y falla si una consulta filtra por clínica sin acotar por profesional. Si agregás una, acotala con un scope de `visibilidad.go` **o** sumala a `clinicaWide` en ese test con el motivo. Se rompió cuatro veces buscando por patrón en vez de por ruta; el estado **medido** el 2026-09-21 es **40 consultas acotadas y 21 excepciones declaradas** (en 6 archivos) — medido instrumentando el test, no contado a ojo, que es como este número se quedó viejo la vez anterior.
- **TODO lo de gestión de clínica es individual** (Fase 3.2.5, barrido del 2026-09-14): además de turnos y pacientes, van por scope los **conflictos de identidad** (`soloMisConflictos`, derivado del turno que los originó), los **tipos de consulta por id** (el listado ya lo hacía; la escritura no), las **tarjetas de General**, los **pendientes de asistencia** y el **enlace para compartir** (`enlaces_turno.user_id` — los turnos que entran por él van a quien lo generó, no al owner). Lo que queda clínica-wide a propósito: los bloqueos de mail/IP del formulario público y la unicidad de DNI.
- **La agenda es de CADA PROFESIONAL, no de la clínica** (Fase 3.2.5, corregido 2026-09-14): horario de atención y horarios reservados se filtran con `soloMiAgenda` (`internal/http/visibilidad.go`) y se crean con `user_id`. `calcularDisponibilidad` recibe el profesional además de la clínica, y filtra horarios, bloqueos **y turnos** por él — contarlos por clínica reintroducía en la pantalla el mismo bug que la 3.2.1 sacó del `EXCLUDE`. El índice `idx_horario_atencion_general_por_profesional` reemplaza al que imponía una fila general por clínica.
- **Un turno cargado desde el panel es de QUIEN LO CARGA, si atiende** (`profesionalQueAtiende`, corregido 2026-09-14). Hasta esa fecha era siempre el owner —un provisorio de la 3.2.1 que caducó en la 3.2.4 sin que nadie lo mirara— y el turno del colega, con su paciente, aparecía en la agenda del titular. El aislamiento no se rompe solo por una query mal filtrada: también por el dato que esas queries leen (`atendido_por_user_id`). **Para recepción es el profesional en foco desde la 3.2.6**, y sin foco un 409: ahí termina el provisorio.
- **`onboardingCompletado` se DERIVA (perfil + clínica activa), no sale de `onboarding_step`** (corregido 2026-09-14): ese paso solo se marca al crear una clínica propia, y desde la 3.2.3 eso dejó de ser obligatorio — un invitado quedaba rebotado para siempre. La columna sigue sirviendo para saber en qué paso retomar el wizard.
- **Recepción puede pararse en la vista de un profesional: el FOCO** (`sessions.viendo_user_id`, Fase 3.2.6, TR-160). Sin foco ve la clínica entera (la vista general); con foco, la sesión se comporta **exactamente** como ese profesional en los seis scopes. Entra por `veTodaLaClinica` y `profesionalEnFoco` en `visibilidad.go` — **si agregás una consulta del panel, no tenés que hacer nada especial: alcanza con usar los scopes de siempre**. Lo mismo para las escrituras: lo que guarda "de quién es esta fila" (`user_id`, `creado_por_user_id`) va con `profesionalEnFocoOpcional`, no con el usuario de la sesión. Y si tu endpoint escribe en una agenda, usá `profesionalParaEscribir(w, r)`: sin foco responde 409 pidiendo elegir vista, porque adivinar (caer al owner, como hacía el provisorio de la 3.2.1) es cómo un turno terminaba en la agenda equivocada. Solo `recepcion` tiene foco; para cualquier otro rol nada de esto existe.
- **La disponibilidad de un turno que YA EXISTE sale de la agenda de quien lo atiende**, no del foco (QA de la 3.2.6). `listDisponibilidadAction` recibe un cuarto argumento con `atendidoPorUserId`, y solo hace falta desde la vista general —sin foco el backend responde 409 y el modal de edición mostraba "no hay horarios disponibles" sobre una agenda libre—. Misma familia que los bugs de configuración: **si tu pantalla pregunta por una agenda, decile CUÁL; el foco solo sirve cuando el dato todavía no tiene dueño.**
- **El aislamiento entre colegas vive en `soloMisTurnos` / `soloMisPacientes`** (`internal/http/visibilidad.go`, Fase 3.2.2, TR-138), no en cada handler: están aplicados en 16 consultas de turnos y 3 de pacientes —contados el 2026-09-21; los números anteriores (6 y 3) quedaron viejos sin que nada avisara, así que contalos antes de citarlos—, y una fuga de aislamiento **no se nota mirando la pantalla** (los datos aparecen, solo que de más gente). Cualquier query nueva que liste turnos o pacientes del panel tiene que pasar por esos scopes. **Ve toda la clínica SOLO `recepcion`** (brief: "acceso a todas las vistas de los N profesionales"); `owner`, `admin` y `profesional` ven lo suyo — el titular es `owner`+`admin`+`profesional` y atiende pacientes como cualquier otro. **`admin` habilita solo la página pública** (`/personalizar-pagina`), nada de agendas. La ficha de un paciente ajeno da **404, no 403**.
- **La clínica activa se elige y vive en la SESIÓN** (`sessions.clinic_id`, Fase 3.2.3, TR-139). `membresiaDeLaSesion` (`internal/http/middleware.go`) la busca JUNTO con la membresía activa —si sacaron a la persona del equipo, la elección deja de valer sola— y cae a "la más antigua" si no eligió ninguna. `/me` devuelve la clínica **activa**, no la propia. El punto de partida de toda sesión es `/clinicas` (`apps/web/src/app/clinicas/`), no `/seleccionar-servicio`.
- **Cada tarjeta de "¿Qué necesitás hoy?" depende de un rol** (Fase 3.2.4): agenda → `profesional`/`recepcion`, página → `admin`, colaboradores → `owner`. **Y la pantalla verifica el rol, no solo la tarjeta**: `/personalizar-pagina` redirige a quien no es `admin`. Esconder el botón nunca fue cerrar la puerta. Los roles completos vienen en `me.clinica.roles` (el viejo `me.clinica.rol` es solo el de mayor alcance, para etiquetar).
- **`admin` es "administrador de PÁGINA", no de la clínica** — habilita `/personalizar-pagina` y nada más. Es invitable y delegable desde Colaboradores (Fase 3.2.4); `PUT /equipo/miembros/{userId}/roles` recibe el juego COMPLETO de roles, no un agregado, y `owner` no se puede asignar por ahí.
- **La matrícula es ÚNICA en todo el sistema** (`idx_matricula_unica`, TR-141): índice único **parcial** sobre `(matricula_tipo, matricula_numero)`, con `WHERE matricula_numero <> ''` porque los perfiles `actividades` la guardan vacía y todas esas cadenas vacías chocarían entre sí. El tipo va en la clave porque `nacional 1234` y `provincial 1234` son registros distintos. Los dos caminos de escritura del perfil (`PATCH /onboarding/perfil` y `PATCH /me`) traducen el 23505 a **409**, no a 500 — con `esMatriculaDuplicada`, que mira el nombre de la constraint y no el texto del error. **Limitación escrita a propósito:** una matrícula provincial es única por provincia y el perfil no guarda cuál; se acepta mientras el producto sea de Córdoba. Los fixtures de test derivan la matrícula del mail (`matriculaDePrueba`) — una constante hace fallar a cualquier test que arme dos personas.
- **Los campos de formulario usan `border border-linea bg-hueso`** (`--color-linea`, #dbd3c2), no `border-[0.5px] border-arena bg-marfil`: a medio píxel y tres puntos de luminosidad, el borde desaparecía sobre el marfil de un modal. Un campo tiene que leerse como un hueco donde escribir.
- **El ESTADO que ve el profesional son cuatro, y dos salen del reloj** (`estadoDeTurno` en `lib/turno-format.ts`, Fase 3.2.7e, TR-158). La base guarda `agendado`/`cancelada`; la pantalla dice **pendiente** (confirmado, no empezó), **en proceso** (estamos dentro de su horario, azul `acero`), **resuelto** (la hora de fin ya pasó) y **cancelada**. Derivado y no persistido a propósito: guardarlo pediría un trabajo periódico que cambie filas solo, y la pantalla igual no se enteraría hasta el próximo sondeo. **"Pendiente" es el rótulo nuevo de `agendado`** —la columna y la URL `?estado=agendado` no cambiaron— y **no tiene nada que ver con el estado `pendiente` que TR-104 eliminó**, que era un turno sin horario fijo. Si mostrás el estado de un turno, usá ese helper y su diccionario, no `t.estado` crudo.
- **La asistencia se puede marcar desde 5 minutos ANTES del turno** (`AnticipoAsistencia` en `turnos.go`, Fase 3.2.7e), y lo que se escribe antes es un BORRADOR en una columna aparte (`turnos.asistencia_preliminar`, TR-159). **Son dos cosas distintas y por eso son dos columnas**: mientras el turno no terminó el borrador es reversible y no dispara nada; `asistencia` sigue siendo irreversible (TR-092) y dispara consecuencias destructivas —resuelve el conflicto de identidad de ese turno, y un "ausente" puede BORRAR la ficha de un paciente sin verificar—, que no pueden colgar de algo que el usuario todavía puede cambiar. El borrador se vuelve definitivo cuando el turno cruza su hora de fin, y lo hace `aplicarLosBorradoresQueVencieron` dentro del sondeo de `/turnos/pendientes-asistencia` (escribe desde un GET, mismo patrón que `/equipo/presencia`). Los dos caminos escriben por `aplicarAsistencia`: no los separes, la parte irreversible tiene que ser idéntica. La ventana se abre antes y **no se cierra nunca** (marcar tarde siempre estuvo permitido). Los botones de la tarjeta de "Turnos de hoy" son el reflejo de esa constante, no la regla — el límite vive en el backend. **Marcar NO adelanta el turno**: sigue en `turnosHoy` con su marca hasta que pase su hora de fin, y el estado sigue saliendo del reloj. Lo que separa `turnosHoy` de `turnosResueltos` es `hora_fin` (una pide `>= ahora`, la otra `< ahora AND asistencia IS NOT NULL`), no la marca — así son complementarias y un turno nunca está en las dos.
- **Para leer la hora en un Client Component usá `useAhora`** (`lib/reloj.ts`, Fase 3.2.7e), no `Date.now()` en un `useState` con efecto: eso dispara renders en cascada (`react-hooks/set-state-in-effect`, error de lint real) y rompe la hidratación, porque en el servidor `Date.now()` es el reloj del contenedor. `getServerSnapshot` devuelve `null` — el HTML del servidor sale sin nada que dependa de la hora.
- **El gesto de "mantener apretado" es UN componente** (`components/panel/boton-mantener-apretado.tsx`, 5 s desde 2026-09-19): lo usan el cartel de asistencia y la tarjeta de "Turnos de hoy". Es la misma acción irreversible; dos implementaciones divergirían justo en el detalle que importa.
- **Un Client Component del panel que reciba datos del servidor los toma con `useEstadoDelServidor`, no con `useState`** (`apps/web/src/lib/estado-del-servidor.ts`, Fase 3.2.7d, TR-156). El inicializador de `useState` corre UNA sola vez: las props frescas que baja `router.refresh()` se descartan, y la pantalla solo se actualiza con F5. Esa única causa explicaba tres síntomas reportados por separado (el conflicto resuelto que seguía listado, la clínica que no cambiaba, el turno editado que no se veía). El hook resincroniza DURANTE el render —no con un `useEffect`, que dejaría un frame con los datos viejos y choca con `react-hooks/set-state-in-effect`—. **No va en un editor ni en un formulario a medio llenar** (`pagina-editor.tsx` está afuera a propósito): ahí el estado local es lo que la persona está escribiendo. Un `useState(props.algo)` en el panel es un bug latente que no se nota mirando la pantalla.
- **La tuerca del header se muestra donde NO se muestra el componente de colaboradores** (Fase 3.2.7e). Ese componente vive en `/panel/**`, `/seleccionar-servicio`, `/colaboradores` y `/personalizar-pagina` (`mostrarColaboradoresEnHeader` en `lib/site-routes.ts`) y su menú ya ofrece "Tu perfil" y "Cerrar sesión"; quedan con tuerca `/perfil` y `/clinicas`. Por eso **"Tu perfil" ya no está en el sidebar del panel**. Y la fila del header es ancha (`w-full px-8`) en TODA pantalla de herramienta con sesión: centrada a `max-w-5xl` el botón redondo de la derecha quedaba en dos X distintas y saltaba al navegar.
- **Una regla que depende de NAVEGAR va donde vive el estado, no en un componente que se desmonta** (2026-09-19). El drawer mobile del panel se cierra en cada cambio de ruta, y eso vive en `PanelSidebarProvider` (layout raíz) y no en `PanelSidebar` (`app/panel/layout.tsx`): ese componente **se desmonta al salir de /panel**, que era justo la navegación a atender — se iba con el drawer abierto y, como el provider sobrevive, al volver aparecía desplegado.
- **El header vive en el layout RAÍZ: no se re-renderiza al navegar.** Cualquier cosa del header que dependa de la ruta (el topbar de `/panel`, Fase 3.2.5) tiene que resolverse en el CLIENTE con `usePathname` y pedir sus datos por Server Action — ver `components/panel/panel-topbar.tsx`. Hacerlo en un Server Component del layout funciona solo con F5 parado en la ruta.
- **Cuál es la clínica activa lo dice `/me`, no el flag `activa` de `/me/clinicas`** (Fase 3.2.5): ese flag vale `true` solo si la sesión la eligió a mano (`sessions.clinic_id`), y quien entró por el fallback de "la más antigua" tiene todas en `false`. Si una pantalla necesita saber en qué clínica está parada, sale de `/me`.
- **La presencia de colaboradores sale de `sessions`, no de un socket** (Fase 3.2.5, TR-142): `last_seen_at` + `clinic_id` + no revocada + no vencida. `GET /equipo/presencia` es **el latido Y la lectura**: refresca el último latido de quien pregunta antes de responder, así que no hay endpoint de "sigo acá". El umbral (5 min) y el intervalo (60 s) los sirve el backend en la respuesta — no los dupliques como constantes del frontend. Si agregás algo de tiempo real, este es el patrón: estado en Postgres, no en memoria del proceso, porque es lo que deja escalar a N instancias.
- **Las reglas que protegen al paciente miran el DNI, no la ficha** (`fichasDeLaPersona`, TR-147). Mientras hay un conflicto de identidad abierto, una persona tiene MÁS DE UNA ficha: cada pedido con un mail desconocido crea una duplicada, y `crearOBuscarPacientePorDNI` saltea las que están `en_conflicto`. Por ficha, el no-solapamiento y el tipo único se salteaban solos justo en el caso más común. Si escribís una regla nueva sobre "el paciente", pasala por ese helper.
- **"Consulta general" y "Urgencia" son la excepción del tipo único** (TR-147): entre profesionales DISTINTOS no chocan — vienen precargadas en todos (`db.SeedTiposConsultaDefault`, que ahora corre también al aceptar una invitación y al recibir el rol desde Colaboradores, idempotente). Con el mismo profesional siguen sin poder repetirse.
- **Una ficha manual SIN MAIL es irreconocible para siempre** (TR-147): `pacienteEstaVerificado` la da por verificada solo por ser `origen = manual`, y `pacienteRespondeAlMail` no puede dar true sin mail contra qué comparar — así que cada pedido público con ese DNI dispara un conflicto nuevo. Por eso el mail es **obligatorio** en "+ Agregar turno > paciente nuevo" y "+ Agregar paciente" (con tutor sigue opcional: ahí la identidad la aporta el tutor).
- **Un conflicto se ve donde se puede resolver, y se resuelve una sola vez** (TR-148). Visibilidad: quien tiene la ficha duplicada entre sus pacientes, EN UNIÓN con quien originó el ticket (sin la unión, un ticket sin turnos vinculados se vuelve invisible para todos). Resolución: alcanza a todos los tickets pendientes de la misma persona **con el mismo mail** (`conflictosDelMismoMail`) — cada pedido crea su propia ficha duplicada, así que cerrar solo los de la misma ficha no alcanza. Bloqueo de asistencia: **solo del lado en disputa**, nunca los turnos de la ficha verificada. Y `soloConflictosVivos` (la ficha duplicada tiene que existir) va en los tres lugares —contar, listar y resolver— o el contador avisa de algo que la pantalla no muestra.
- **El enlace compartido decide con quién y para quién** (Fase 3.2.7b, TR-149). `enlaces_turno.para_todos_los_profesionales` (false = agenda del que lo generó, el wizard no pregunta; true = elige el paciente) y `enlaces_turno.paciente_id` (con ficha, el wizard salta los pasos de datos que esa ficha ya tiene). **El mail del paciente NO viaja en la respuesta pública de validación**: se resuelve desde la ficha con `identidadDeLaFichaDelEnlace`, y solo si el enlace trae EXACTAMENTE esa ficha — un enlace se reenvía.
- **El wizard público elige TIPO y después PROFESIONAL** (Fase 3.2.7, TR-146). `GET /clinicas/{slug}/tipos-consulta` devuelve **nombres deduplicados** —sin `id` ni `duracionMinutos`: con N profesionales no existe "el id del tipo", existe la fila de cada uno— y **solo los que atiende al menos un profesional activo**. `GET /clinicas/{slug}/profesionales?tipo=<nombre>` devuelve quiénes lo atienden, cada uno con SU `tipoConsultaId`, su duración y el primer día con hueco (ventana de 30 días, tope duro: el endpoint es público y sin sesión). **Una sola función resuelve tipo + profesional para los tres endpoints públicos** (`tipoPublicoDelPedido`): horarios de un día, días del mes y alta. Si los separás, volvés a ofrecer los huecos de uno y agendar con otro — era un bug real del camino con enlace. Con enlace manda el dueño del enlace y la lista de tipos se acota a los suyos. **Nunca caer al mail para nombrar a un profesional en la página pública** (`nombresPublicosDeProfesionales`, no `nombresDeLosMiembros`).
- **Un tipo de consulta es de UN profesional** (Fase 3.2.5): `GET /tipos-consulta` devuelve los tuyos (`user_id = vos`, más los de `user_id` nulo, anteriores a la 3.2.1); los de los colegas están en `/tipos-consulta/de-colegas`, y se ofrecen como **punto de partida dentro del modal de alta** ("Tipos de consulta ya creados"): elegir uno rellena nombre y color, y el tipo se crea por el alta de siempre con los tiempos que configure la persona. Nunca compartas la fila: duración y preferencia horaria son configuración de agenda, y cambiarla le movería los huecos del día a otro. Al crear uno, ponele `user_id`.
- **Crear una clínica `organizacion` termina en `/colaboradores`**, no en `/seleccionar-servicio` (`individual` sigue yendo ahí). Esa pantalla es entonces un punto de llegada, no solo un desvío: tiene al final un botón "Ir al panel de gestión" que vuelve a `/seleccionar-servicio` (no a `/panel`, y por eso sin candado por rol — esa pantalla ya filtra las tarjetas sola).
- **Una invitación se dirige a un MAIL, no a un usuario** (`clinic_invitations`, Fase 3.2.4, TR-140): por eso se puede invitar a alguien sin cuenta, y por eso las invitaciones se buscan por `email` y no por `user_id`. **Las dos formas de invitar —código de perfil y mail— quedan pendientes de confirmar**: la membresía recién nace cuando la persona acepta desde `/clinicas`. No hay link de aceptación de un clic, a propósito. Aceptar una invitación de `profesional` exige matrícula, igual que crear la clínica propia o entrar a atender.
- **No todo el que entra a la app atiende pacientes** (Fase 3.2.3, TR-139). `professional_profiles.tipo_perfil` es `profesional` o `actividades` (recepción / administración de la página); con `actividades` **no hay matrícula ni especialidades**, y el backend las descarta si llegan. Default `profesional` cuando el campo no viene. **Crear la clínica propia exige `profesional`, y entrar a una clínica con rol de `profesional` también** — el frontend pide los datos que faltan antes, en un modal encadenado, y los dos caminos tienen su guard en el backend. Crear una clínica exige además provincia, ciudad, dirección y teléfono: son los datos de la página pública y del buscador.
- **El onboarding es UN paso: el perfil** (Fase 3.2.3). Crear la clínica dejó de ser obligatorio —se entra a la app también porque un colega te sumó a la suya— y pasó a ser una acción de `/clinicas`. Crear la propia ya no exige estar en el paso `clinica` del wizard; lo que se rechaza es tener dos (**409**, no 403).
- **`soloMisPacientes` tiene TRES criterios, y el tercero es un acto** (`pacientes_en_mi_lista`, Fase 3.2.7d, TR-157): tengo turnos con esa persona, yo cargué la ficha, **o** la sumé a mi lista desde "+ Agregar paciente > De la clínica". Los dos primeros son hechos derivados; sin el tercero, la única forma de sumar a alguien que ya existía en la clínica era inventarle un turno. Es una tabla y no una columna porque son N profesionales por ficha. Si agregás otra pantalla que pregunte "¿esta ficha es mía?", usá los tres — `esMio` del buscador de la clínica miraba solo los turnos, y las dos pantallas decían cosas distintas sobre la misma ficha.
- **UNA FICHA SIN DUEÑO NACE INVISIBLE** (QA de la 3.2.6). `creado_por_user_id` es uno de esos tres criterios: una ficha cargada sin él queda en la clínica y **fuera de la lista de todos** hasta que alguien le invente un turno. Por eso "+ Agregar paciente" pide **"Profesional · Obligatorio"** antes que cualquier otro dato y el backend lo exige con un 409 (para un profesional se autoelige él mismo; recepción es la única que decide). Del mismo par de preguntas sale la columna **"Profesionales"** de la tabla: `profesionalesPorPaciente` usa **los mismos tres criterios leídos al revés** —allá "¿esta ficha es mía?", acá "¿de quiénes es?"— y si divergen, la columna afirma que un paciente es de alguien que no lo ve en su propia lista. **Y "De la clínica" no se ofrece en la vista general**: esa pestaña suma a MI lista una ficha que ya existe, y ahí no hay lista propia a la que sumarla.
- **La vista del recepcionista es un PROFESIONAL EN FOCO, no un módulo aparte** (`sessions.viendo_user_id`, Fase 3.2.6, TR-160). Sin foco es la vista general de la clínica; con foco la sesión se comporta como ese profesional en las cuatro pantallas. Los seis scopes de `visibilidad.go` ya se bifurcaban en `veTodaLaClinica(r)` y ya resolvían un usuario, así que cambiar qué responden esas dos preguntas hizo funcionar las 25 consultas del panel sin tocar una. **Las escrituras siguen el foco igual que las lecturas**: todo lo que guarda `user_id` guarda *de quién es la fila*, no quién apretó el botón (la única excepción deliberada es `esVos` de `perfil_colega.go`). Un profesional que intenta mirar a un colega recibe **403**, no un selector escondido.
- **MIRAR y CARGAR son dos preguntas, con dos controles del mismo dibujo** (QA de la 3.2.6). `ZonaProfesional` cambia el foco de la sesión (cabecera de cada pantalla, y arriba de "Configuración de calendario", que *necesita* mover el foco porque lee con los scopes); `SelectorDeAgenda` no toca la sesión y solo elige el destino de lo que se está por crear ("+ Agregar turno", el atajo "Reservar horario", "Compartir link"). Los dos envuelven a `CarruselDeProfesionales`. El campo es `profesionalUserId` en `POST /turnos` / `POST /bloqueos` / `POST /enlaces-turno`, y **quién puede usarlo lo decide `puedeCargarEnLaAgendaDe`** — recepción cualquier profesional activo, el resto solo el propio, con **404** (para quien no puede, esa agenda no existe).
- **El nombre del profesional en las tarjetas y en `/turnos` es EXCLUSIVO de la vista general** (QA de la 3.2.6): en la vista de un profesional todos los turnos son suyos y la columna sería su nombre repetido. La condición vive en el backend (`veTodaLaClinica`), no en la pantalla. **Gatear el nombre no es gatear la función**: `completarProfesionalDeTurnos` toma `conNombre` porque también completa `esMio` (si el turno se puede tocar) y el nombre del tipo de consulta — saltearla entera dejaba a recepción con todos los turnos en solo lectura.
- **LA CONFIGURACIÓN DE AGENDA ES DE UNA PERSONA, y recepción sin foco NO tiene agenda** (`duenioDeLaAgenda` / `agendaAConfigurar` / `soloDeLaAgendaDe`, QA de la 3.2.6). Para una LISTA de turnos "toda la clínica" significa algo; para el horario de atención, los horarios reservados y los tipos de consulta, no. Los endpoints de configuración responden **409** antes que escribir una fila sin dueño — y una fila con `user_id` NULL no es "de la clínica": los scopes la muestran en la agenda de TODOS (son las filas anteriores a la 3.2.1). Si agregás un endpoint de configuración, resolvé la agenda con `agendaAConfigurar` y rechazá el caso vacío.
- **Cuando una subfase cambia quién es "yo", buscá el concepto viejo en TODO el paquete** (QA de la 3.2.6). `tipos_consulta.go` y `tipos_consulta_colegas.go` se quedaron en `session.UserID` cuando la 3.2.6 mudó el resto al foco: recepción veía siempre la misma lista —la de alguien que no atiende—, el tipo que creaba nacía a su nombre y después no lo encontraba para editarlo ("tipo de consulta no encontrado"). No se nota mirando la pantalla propia; se nota en QA.
- **Los conflictos: AISLAR no es ESCONDER** (QA de la 3.2.6). Son dos reglas distintas y conviene nombrarlas aparte: el **aislamiento** dice que cada turno se compara solo contra su propia agenda (`turnoChocaConSuAgenda`, y el agrupado por columna del banner del calendario); la **visibilidad** dice que recepción se entera de CUALQUIER conflicto de la clínica, esté parada donde esté. Un cambio que "arregla el aislamiento" y de paso hace desaparecer datos de la pantalla está mezclando las dos. El contador cuenta también las **excepciones de horario**, y devuelve el día y el dueño del más próximo para que el aviso pueda llevar hasta ahí.
- **Dos avisos de conflicto, dos trabajos** (QA de la 3.2.6): el global (`NotificacionesConflictoGlobal`) vive FUERA del calendario y lleva hasta el día y la agenda del problema; el del calendario abre la pantalla de resolución y **no desaparece al cambiar de día** porque su número sale del servidor, no de los clusters de la vista cargada. Mostrar los dos en la misma pantalla se probó y se descartó: confunde.
- **El calendario es DUEÑO de su rango de fechas: ahí `useEstadoDelServidor` hace daño** (QA de la 3.2.6). Es la excepción a TR-156. Esa pantalla pide el rango que la persona está mirando, y el prop del servidor describe siempre el rango inicial: con el hook, cada `revalidatePath` pisaba lo recién traído y el turno autoreservado (movido a otro día) o el turno de un solapamiento nuevo desaparecían del estado. **El prop es una SEÑAL de que algo cambió, no el dato**: se escucha su cambio de identidad y se vuelve a pedir el rango visible. Todo fetch de cliente de esa pantalla lleva además `vistaKey` en sus dependencias, o se queda con los datos del profesional anterior.
- **Una columna DATE (`fecha_desde`/`fecha_hasta`) llega como medianoche UTC**: pasarla por `clock.In` la corre al día anterior y la regla deja de cubrir su propio día (bug real, QA de la 3.2.6). Se formatea con `.Format("2006-01-02")` directo, como hace `formatFechaPtr`. `clock.In` es para INSTANTES, no para fechas de calendario.
- **El cartel de asistencia es del profesional, no de recepción** (QA de la 3.2.6): es un modal incerrable y marcar dispara consecuencias irreversibles, así que esa decisión es de quien atendió. La regla vive en `/turnos/pendientes-asistencia` además de en el layout que lo monta. Recepción sigue marcando por adelantado desde "Turnos de hoy" (un borrador reversible), y **un turno que termina sin marcar ya no se pierde**: pasa a "Turnos resueltos" como *asistencia pendiente*. Como el sondeo del cartel era lo único que aplicaba los borradores vencidos, `/panel/resumen` también los aplica.
- **En la vista general, `/tipos-consulta` devuelve los de TODA la clínica** (QA de la 3.2.6). Devolvía vacío, y el calendario se quedaba sin con qué resolver el tipo de cada turno: la tarjeta decía "—" y se pintaba gris. Cada turno trae el id de la fila de SU dueño (TR-145), así que con la lista completa cada uno encuentra su nombre.
- **RECEPCIÓN TIENE SU PROPIA PALETA, precargada** (`lib/paleta-recepcion.ts`, QA de la 3.2.6). El color de un tipo de consulta es de cada profesional, y TR-145 dice que un punto de color significa lo que decidió QUIEN MIRA. En la vista general esa regla se queda sin quien mire: pintar cada turno con el color de su dueño mezcla paletas que nadie coordinó —el verde de uno puede ser "Limpieza" y el de otro "Urgencia"—. Así que ahí el color se asigna **por NOMBRE** desde una paleta fija: las dos filas de "Limpieza dental" se ven iguales, porque para recepción son la misma cosa. Vale en **todas sus pantallas**, no solo en la vista general: parada en la agenda de un profesional, recepción sigue percibiendo las consultas con su color — el del profesional es exclusivo de él. Entra por `tiposConsultaDeLaVista` (`lib/tipos-de-la-vista.ts`), que es por dónde las cuatro pantallas piden los tipos, así que todo lo de abajo sigue resolviendo por id y no sabe nada de esto. **La excepción es "Configuración de calendario"**: ahí recepción está editando la configuración de ese profesional y el color que ve es el que va a guardar, así que se muestra el REAL (esa pantalla los pide por su cuenta, del lado del cliente).
- **La vista general del calendario es exclusiva de "Día"** (QA de la 3.2.6): ahí hay una columna por profesional. En Semana las siete columnas ya son los días, y 7 × N no se lee. Al pasar a Semana/Mes se elige solo al dueño del primer turno del día y desde ahí no se vuelve a la general. Por eso el selector lo dibuja `CalendarView` y no la página: lo que puede ofrecer depende de Día/Semana/Mes, que es estado del cliente.
- **El perfil de un colega se lee por `GET /equipo/miembros/{userId}/perfil`** (Fase 3.2.7d), acotado a miembros **activos** de la clínica de quien pregunta: 404 —no 403— para cualquier otro uuid, mismo criterio que la ficha de un paciente ajeno. Viaja lo mismo que el perfil propio **menos el documento**. Y los roles necesitan `Preload("Roles")`: viven en `clinic_member_roles`, no en una columna — sin eso la lista vuelve vacía en silencio.
- **Una ficha de paciente cargada a mano guarda `creado_por_user_id`**, y `soloMisPacientes` la incluye por eso: sin esa columna, un profesional cargaba una ficha con "+ Agregar paciente" y desaparecía de su listado hasta tener el primer turno. No lo vuelve dueño del paciente (siguen siendo de la clínica).
- **`requireClinic` admite cualquier membresía ACTIVA, no solo al dueño** (Fase 3.2.2). Exigía `owner`, y eso dejaba a cualquier colaborador invitado con 403 en todo el panel. Deja en el contexto la clínica **y los roles**; con varias clínicas toma la más antigua hasta que la 3.2.3 traiga la elección explícita. Para exigir un rol en una ruta: `requireRol(db.RoleAdmin)` encadenado después. Para preguntar desde un handler: `tieneAlgunRol(r, ...)`.
- **El titular arranca con `owner` + `admin` + `profesional`**, como pide el brief. `owner` no es un nivel más alto: es lo que habilita a invitar y repartir roles. Si agregás una ruta que exija `admin`, acordate de que un profesional invitado NO lo tiene.
- **Los roles de un miembro viven en `clinic_member_roles`, no en una columna** (desde la Fase 3.2.1). Son acumulables, y la exclusión `profesional`/`recepcion` la impone un **índice único parcial**, no la aplicación. Para filtrar por rol usar el scope `db.ConRol(...)`; para mostrar uno solo, `db.RolPrincipal(...)`. Asignar con `db.AsignarRol` (idempotente). **La membresía no se borra nunca**: se marca `status='removed'`, porque la FK compuesta de `turnos` bloquearía la baja de cualquier profesional con historial.
- **Los pacientes son de la CLÍNICA; la configuración de agenda es del PROFESIONAL.** Una ficha por persona aunque la atiendan dos odontólogos ("los pacientes de Lucía" sale de los tres criterios de `soloMisPacientes`); tipos de consulta, horario de atención y bloqueos cuelgan del profesional, no de la clínica.
- **Los tipos de consulta se comparten como sugerencia, no como fila común**: incluir el de un colega COPIA la fila, porque lleva duración/color/preferencias que cada uno ajusta. **Un tipo se identifica por su NOMBRE** (TR-145): "Limpieza dental" es lo mismo en toda la clínica, el color y los minutos son de cada agenda. De ahí tres reglas que van juntas — no se puede crear dos veces el mismo (409), lo que ya tenés no se ofrece como punto de partida, y el turno viaja con `tipoConsultaNombre` —**el nombre y nada más**— para que la ficha del paciente no muestre "—" en el turno de un colega. **El color NO viaja**: lo resuelve la pantalla contra los tipos de quien mira, buscando por ese nombre, y queda neutro si no lo tiene. Si tu colega tiene "Consulta general" en verde y vos en beige, en tu ficha se pinta beige — un punto de color significa lo que decidió quien mira, no quien cargó. **Bloquear usa igualdad exacta normalizada; esconder usa el fuzzy `seParecen`** — no unificarlos: un falso positivo que esconde cuesta una sugerencia, uno que bloquea es una pared ("Tipo de prueba 1" y "Tipo de prueba 2" dan 0.94). El repertorio de `tipos_consulta_catalogo.go` es una lista de SUGERENCIAS, no una semilla de la base.

## Fase 4 — Personalización de la página pública

Pedido directo del cliente (2026-09-17, sin brief `.docx`), primer entregable de un revamp de frontend más amplio — el resto (UI del panel de gestión) queda para después, todavía sin fases numeradas. Definición funcional completa en `docs/Fases post MVP/Fase 4/fase4-personalizar-pagina.md`; estado de la implementación (para retomar) en `docs/Fases post MVP/Fase 4/fase4.4-4.5-estado-implementacion.md` (la 4.1 a 4.3 está en `fase4.1-a-4.3-estado-implementacion.md`, mergeada). Decisiones: `docs/Arquitectura y base/tradeoffs.md` TR-150 a TR-155; plan: `implementation-plan.md` §14. **Las subfases 4.4 y 4.5 entraron a `dev` como "etapa intermedia" (PR #44, 2026-09-18): el cliente va a sumar secciones extra de fine-tuning todavía sin definir, así que la Fase 4 no está cerrada.**

| Subfase | Qué | Estado |
|---|---|---|
| 4.1 | Modelo de datos: `PaginaPublica` extendida (bio, tema/variante/tipografía, foto de portada, redes sociales, mapa, dirección override) + tabla `PaginaPublicaModulo` | ✅ |
| 4.2 | Endpoints backend y frontend: `GET/PATCH /panel/pagina` (reemplazo completo, mismo criterio que `PUT /horario-atencion/general`), `POST /panel/pagina/fotos` (primer upload multipart del repo), estadísticas reales (pacientes atendidos/turnos realizados, nunca un valor cargado a mano), y el fix del bug owner-only (especialidades/búsqueda por nombre pasan a ser la unión de TODOS los profesionales activos, no solo el owner) | ✅ |
| 4.3 | Catálogo de temas: 5 temas × 3 variantes de color cada uno + 5 pares de tipografía **compartidos entre temas** (no uno por tema, para no cargar hasta 15 pares de Google Fonts) — validado con CHECK en Postgres (`chk_pagina_publica_tema*`) y en el handler (`temaEsValido`, que sí valida la relación tema↔variante — el CHECK de la base valida cada columna contra su propio catálogo plano, no esa relación cruzada) | ✅ |
| 4.4 | Editor: borrador único que se guarda de una vez, módulos reordenables (`@dnd-kit` + flechas), selector de tema, subida de fotos, vista previa móvil/tablet/escritorio | ✅ mergeada a `dev` (PR #44), sin QA en navegador |
| 4.5 | `ClinicaPublicaTemplate` dibuja el tema y los módulos persistidos | ✅ mergeada a `dev` (PR #44), sin QA en navegador |
| 4.6 | Storage real en producción (Cloudflare R2, TR-046) — hoy `internal/storage` solo tiene `LocalStorage` (disco, dev); `STORAGE_R2_BUCKET` configurado hace fallar el arranque a propósito en vez de degradar en silencio a un storage que se pierde en cada deploy | pendiente |

Lo que hay que saber al tocar la página pública o su editor (TR-151 a TR-153):

- **`@container` va SOLO alrededor de la grilla de módulos.** El wizard de turno y "Mis turnos" abren modales `position: fixed` desde adentro de `ClinicaPublicaTemplate`; un ancestro con `container-type` es su bloque contenedor y los recortaría al ancho de la página. Ningún test de jsdom lo detecta.
- **Editor y plantilla comparten `ContenidoPagina` y `modulosPorDefecto()`** (`lib/pagina-publica/`): una lista vacía es "nunca la personalizaron" (estructura por defecto), por eso el editor no deja quitar el último módulo y el endpoint público devuelve `personalizada` (no devuelve los módulos ocultos).
- **Para vaciar un campo opcional se manda `""`, no `null`** (un `*string` de Go no distingue `null` de ausente); el backend lo guarda como NULL.
- **Todo dato que la página pública convierte en `href`/`src` se valida en el backend Y se filtra en el frontend** (`enlaces.ts`): un `javascript:` cargado por un admin no puede volverse un link.
- **GORM ignora un `false` en un `bool` con `default:true` al hacer `Create`** — ocultar un módulo se guardaba como visible. Se escribe aparte con `Update`.
- **Las fotos locales se sirven same-origin** por `apps/web/src/app/uploads/[...path]/route.ts` (URL relativa `/uploads/x.jpg`, TR-154): la CSP (`img-src 'self' https:`) bloquea imágenes de otro origen o de http, y **jsdom no aplica CSP** — un test unitario no lo ve. El mapa del módulo Contacto entra por `frame-src https://www.google.com/maps`.
- **El nombre de un módulo (`config.nombre`) es una etiqueta del editor, no un título público; el nombre de la clínica puede ir sobre la portada con un color de un set curado** (`lib/pagina-publica/portada.ts`, TR-155). El color y su velo van juntos: sumar uno toca `COLORES_NOMBRE`, `coloresNombreValidos` (Go) y el CHECK `chk_pagina_publica_nombre_color`.
- **Sin storage no hay fotos:** en producción la subida responde 501 hasta la 4.6. `horarios` está aceptado por el backend pero no en el editor (¿de cuál de los N profesionales?).
- **Server Actions tienen 1 MB de body por default**; `next.config.ts` lo sube a 6 MB por las fotos.

Los módulos se guardan con **reemplazo completo** (`DELETE` + `INSERT` transaccional), no un CRUD por módulo — no hay precedente en el repo de un PATCH parcial de un array polimórfico, y calza con que el editor de la 4.4 arma todo el layout en el cliente y guarda de una vez. `Modulos *[]moduloRequest` es un puntero al slice, no el slice solo: distingue "no vino en el body" (no tocar) de "vino `[]`" (borrar todos).

**Continuación: Prisma Engine (plan aprobado, sin implementar).** Kevin definió el 2026-09-21 cómo sigue esta vertical — registro único de módulos, tokens de diseño, variantes, efectos, módulos del rubro (Equipo, Horarios del edificio, Servicios), plantillas, borrador/Publicar con historial y SEO — en `docs/Fases post MVP/Prisma Engine/plan-prisma-engine.md`. **Ver el aviso "Zona en obra" al principio de este archivo antes de tocar la página pública, su editor o el perfil del profesional.**

## Flujo de ramas

Mismo criterio que Marcuzzi_Madryn: `main` (prod) ← solo merge cuando el usuario lo pide explícitamente ("mergeá") · `dev` (integración, push libre) ← `feature/`/`fix/` para trabajo grande, cambios chicos van directo a `dev`. Merge a `main` siempre vía `git merge --ff-only`. **Ojo: "push libre" no es inocuo** — un push a `dev` corre CI (web, api, test-api con `-race` y gate 80%, test-web) y, si pasa, dispara el deploy en Render (un solo entorno, TR-021); `main` corre el mismo CI pero no despliega.

**Convenciones que el remoto realmente usa** (verificadas con `gh` y el historial de `origin/dev`, 2026-09-21) — no hay `PULL_REQUEST_TEMPLATE`, `CONTRIBUTING`, commitlint ni hooks en el repo, así que esto es costumbre, no una regla que algo haga cumplir:
- **Commits:** Conventional Commits en castellano, `tipo(alcance): descripción` — `feat`/`fix`/`docs`/`test`, con la subfase como alcance (`fix(3.2.6): …`, `feat(4.4-4.5): …`, `docs(panel): …`). La mayoría lleva el trailer `Co-Authored-By: Claude … <noreply@anthropic.com>`. Los docs (TR, plan, spec) suelen ir en su propio commit `docs(...)` aparte del código.
- **PRs:** de una rama `feature/…` o `fix/…` **a `dev`** (nunca a `main` directo), con el título en el mismo formato y el cuerpo en castellano explicando el porqué (secciones `##`, y una advertencia arriba cuando es una etapa intermedia, como el PR #44). Se mergean con **merge commit** (`Merge pull request #N from …`), no squash ni rebase, aunque el repo permite los tres. Una misma rama puede reabrirse en PRs sucesivos (#47, #48 y #49 salen de `feature/fase3.2.6-vista-recepcion`, uno por ronda de QA).
- **Sin barreras en GitHub:** ni rulesets, ni revisión obligatoria (`reviewDecision` vacío en los PRs mergeados), ni auto-merge, y `delete_branch_on_merge` está apagado — por eso `origin` acumula ramas viejas y los locales quedan con el upstream `gone`. Lo único que frena un merge es el CI (`web`, `api`, `test-api`, `test-web`). La cuenta `Kevinmass` tiene permiso de `push`, sin `admin`, así que no puede ver ni cambiar la protección de ramas.
- **Autores:** dos personas mergean en el mismo repo (`JuanMuruzabal`, dueño, y `Kevinmass`); conviene un `git fetch --prune` antes de empezar a trabajar.
- **`main` y `dev` divergieron:** `main` tiene 4 commits que `dev` no (los merges de los PR #17, #25, #26 y un `Update README.md` hecho directo), así que `git merge --ff-only` de `dev` a `main` **falla hoy**. Hay que traer `main` a `dev` primero (o pedirle al usuario cómo quiere resolverlo) — no forzar ni reescribir `main`.

**Fase 2 — proceso de QA por ítem (TR-083 en `docs/Arquitectura y base/tradeoffs.md`):** pedido explícito del cliente en `docs/Fases post MVP/Fase 2/fase2-dental-mirage.md` — implementar los 5 ítems uno por uno, cada uno en su propia rama `feature/fase2-NN-descripcion` (no una sola rama larga para toda la fase). **Nunca hacer commit ni push de un ítem hasta que el cliente apruebe su QA** — a diferencia del resto del proyecto, donde push a `dev` es libre. Una vez aprobado un ítem, commit a su rama y PR a `dev` (no push directo). Este proceso aplica solo mientras dure la Fase 2, no cambia el flujo general de arriba para el resto del trabajo.

## Documentación en Obsidian
Cada vez que se ejecute /init en este repositorio, revisar también la documentación del proyecto en el vault de Obsidian ubicado en C:\Users\kevin\OneDrive\Documents\Kevin\Proyectos\dental-mirage\ y actualizarla si hay cambios relevantes en el código que no estén reflejados ahí.
