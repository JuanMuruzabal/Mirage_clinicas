{
  "name": "@dental-mirage/prisma-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Registro único de módulos de la página pública (Prisma Engine, docs/Fases post MVP/Prisma Engine/plan-prisma-engine.md, PE-1) — un módulo es un paquete autocontenido (meta, editor, render) consumido por apps/web (editor y plantilla). No se compila: apps/web lo transpila vía `transpilePackages`, mismo criterio que @dental-mirage/shared-types.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19",
    "@vitejs/plugin-react": "^6.0.5",
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^30.0.1",
    "react": "19.2.8",
    "typescript": "^5.4.0",
    "vitest": "^4.1.10"
  }
}
