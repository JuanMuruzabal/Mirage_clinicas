import type { DefinicionSlotAnimable } from "../../efectos/catalogo";
import { SLOTS_TARJETA } from "../../efectos/slots";

// Un acordeón es el único formato previsto para preguntas frecuentes en PE-6.
export const SLOTS_PREGUNTAS: readonly DefinicionSlotAnimable[] = SLOTS_TARJETA;
